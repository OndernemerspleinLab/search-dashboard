import { css } from 'styled-components'

export const cleanButtonStyle = css`
	background: none;
	border: none;
	color: inherit;
	font: inherit;
	padding: 0;
	display: block;
	width: 100%;
	text-align: left;
	line-height: inherit;
`
